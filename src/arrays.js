/*
Write a javascript function that will merge all the 
array's elements into a string.  the delimeter is optional
default is ','but could be any string 

note: you will have to use a for while and Array.unshift

example:
--------------------
var elements = ['ya','ba','da','ya','dou']
mergeToStringUsingWhile(elements); // will return 'ya,ba,da,ya,dou'

*/
function mergeToStringUsingJoin(elements, delimiter = ',') {

}

/* // ---------------------------------------------------------------------------

/*
Write a javascript function that will merge all the 
array's elements into a string. the delimeter is optional
default is ','but could be any string 

note: you will have to use a for loop.

example:
--------------------
var elements = ['ya','ba','da','ya','dou']
mergeToStringUsingFor(elements); // will return 'ya,ba,da,ya,dou'

*/
function mergeToStringUsingFor(elements, delimiter = ',') {

}

/* // ---------------------------------------------------------------------------

/*
Write a javascript function that will merge all the 
array's elements into a string.  the delimeter is optional
default is ','but could be any string 

note: you will have to use a for while and Array.unshift

example:
--------------------
var elements = ['ya','ba','da','ya','dou']
mergeToStringUsingWhile(elements); // will return 'ya,ba,da,ya,dou'

*/
function mergeToStringUsingWhile(elements, delimiter = ',') {

}

/* // ---------------------------------------------------------------------------

/*
Write a function who can receive any number and
return a dash between all even numver

ex:
number = 1212349584
return 12-6234958-4
*/
function addDashBetweenNumber(number) {

}

/* // ---------------------------------------------------------------------------

/*
Write a function that receive an arrays of string, find the item
having the most occurences and return an array containing [itemValue, nbOfOccurence]

ex:
elements = ['a', 'b', 'b', 'c', 'a', 'a', 'z']
return ['a', 3]

note : if it have 2 or more item having the same nb of occurence the function should return false
*/
function findMostOccurence(number) {

}

/*
Write a function that receive an multidimentional array of strings
and flatten it to a one dimentionnal array

ex:
elements = ['a', 'c', ['1',['3'],'3'], 'e', ['z'], 'g', 'z']
return ['a', 'c', '1','3','3', 'e', 'z', 'g', 'z']

note : if it have 2 or more item having the same nb of occurence the function should return false
*/
function flatten(number) {

}
